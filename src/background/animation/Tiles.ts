import type { CanvasAnimation } from './types'

export default class Tiles implements CanvasAnimation {
  constructor(public canvas: HTMLCanvasElement, public gl: WebGLRenderingContext) {}
  render() {
    return
  }
  update() {
    return
  }
}
